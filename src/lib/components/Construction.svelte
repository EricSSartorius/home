<p class="text-center text-xs"><strong>🚧 under construction 🚧 </strong></p>

<style>
	p {
		--tint-or-shade: color-mix(in oklab, var(--caution), transparent 85%);
		gap: var(--gap);
		padding: var(--gap-xxs) var(--gap);
		border-radius: var(--circle-radius);
		background-color: var(--tint-or-shade);
		margin: var(--gap-xs);
		position: absolute;
		margin: 0;
		width: 100%;
		border-radius: 0;
	}
</style>
